---
interface Props {
  href: string;
  external?: boolean;
}
const { href, external = true } = Astro.props;
const rel = external ? 'nofollow noopener noreferrer' : undefined;
const target = external ? '_blank' : undefined;
---
<a href={href} rel={rel} target={target}><slot /></a>
