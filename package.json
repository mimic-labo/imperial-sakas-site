{
  "name": "imperial-sakas",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "prebuild": "node scripts/validate-collection.mjs && node scripts/validate-stats.mjs",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "generate-types": "wrangler types",
    "validate": "node scripts/validate-collection.mjs && node scripts/validate-stats.mjs"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^13.3.0",
    "@astrojs/mdx": "^5.0.4",
    "@astrojs/sitemap": "^3.7.2",
    "astro": "^6.2.1",
    "wrangler": "^4.87.0"
  },
  "overrides": {
    "vite": "^7"
  },
  "devDependencies": {
    "gray-matter": "^4.0.3"
  }
}
